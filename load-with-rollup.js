/**
 * Implements interpretation of the npm:protocol in source files and node: as also adds cmake: gcc: cargo: 
 * protocols like vcpkg: can be impl on top of cmake or gcc msys2 support is also planned.
 * This is pre work for the stealify compiler @stealify/compiler we use rollup to parse @stealify/script
 * 
 */
